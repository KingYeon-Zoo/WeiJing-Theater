---
name: dedupe-rules
description: Use when extractor 需要判断角色或场景是复用已有数据还是新增当前集记录。
---

# 去重规则

## 角色

- 同名角色优先视为同一人。
- 若已有角色信息更完整，用新信息补充旧记录，不要新建重复角色。
- 只有明显是不同人物但剧本写法冲突时，才在描述中保留歧义并谨慎处理。

## 场景

- `location + time` 完全一致时，优先复用。
- 同地点不同时段视为新场景。
- 只有地点模糊、但能从剧情明确判断是同一空间时，才允许复用已有地点写法。
